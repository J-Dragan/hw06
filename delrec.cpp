#include "hw06.h"

void deletRecord(const slist* list, const int place){
        sentry* head = list -> first;
                if(list -> length = place){
			head = list -> last;
			sentry* left = head -> prev;

			
		
		
		
		
		
		
		}
                        
